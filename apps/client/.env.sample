# Optional. The client calls /api relatively — in dev Vite proxies it to the API,
# in production the API serves this build itself. Set this only to point a local
# build at a remote API.
# VITE_API_URL=https://crm.example.org/api
