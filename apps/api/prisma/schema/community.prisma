/// A church community. People may belong to more than one community.
model Community {
  id String @id @default(uuid())

  name String @unique

  people Person[]

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@map("communities")
}
